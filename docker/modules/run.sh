#!/usr/bin/env bash
#  The  run.sh  script is a simple bash script that runs the  docker compose up --build  command. This command will build the Docker image and run the application. 

docker compose up --build